# escape the characters #, !, =, and : with a preceding backslash

# (local) address of database
url = jdbc\:mysql\://localhost\:3306/

# database user/password
user = root
password = secret123

# logfile is optional
logfile = log.txt

# optional: authentication password, if provided, modifying the database requires this password to be send as query parameter 'auth'
#authenticationPassword = pwd
